# Turn off startup_message
startup_message off
# Turn off ugly screen flash instead of a bell sound
vbell off
# Remap escape to `  
escape ``
# How to type `
escape `r
# Hardstatus config 
hardstatus alwayslastline
hardstatus string "\033[32m[ %m/%d %c ]\033[0m%=\033[38;5;214m%?%-Lw%?(%n*%t)%?%+Lw%?%=\033[32m[ %H ]\033[0m"
# Change def scrollback value
defscrollback 40000
